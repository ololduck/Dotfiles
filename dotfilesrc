[dotfiles]
repository = ~/dotfiles
ignore = [
    '.git',
    '.gitignore',
    'README.md',
    'Vagrantfile',
    'LICENSE']
packages = [
    'config/xfce4',
    'config/beets',
    'config/mpd',
    'config'] # Simlink everything in <repo>/config/* to ~.config/

# vim:ft=ini:
